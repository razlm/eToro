apiVersion: v1
kind: Service
metadata:
  name: {{ include "etoro.fullname" . }}
  labels:
    {{- include "etoro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "etoro.selectorLabels" . | nindent 4 }}
